{
  "name": "spotify_backend",
  "homepage": "https://kriti001.github.io/Spotify-Clone",
  "version": "1.0.0",
  "description": "this will contain the backend code for the spotify clone",
  "main": "index.js",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -b master -d build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "kritika",
  "license": "ISC",
  "devDependencies": {
    "gh-pages": "^6.1.1"
  }
}
